rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ── Helpers ────────────────────────────────────────────────────

    function isAuth() {
      return request.auth != null;
    }

    function isEmployer() {
      return isAuth() &&
        exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'employer';
    }

    // Returns the caller's email — first from the JWT token, then from their users doc
    function callerEmail() {
      return request.auth.token.email != null
        ? request.auth.token.email
        : (exists(/databases/$(database)/documents/users/$(request.auth.uid))
            ? get(/databases/$(database)/documents/users/$(request.auth.uid)).data.email
            : "");
    }

    // Check if the email field on a document matches the currently signed-in user
    function isOwner(email) {
      return isAuth() && (
        email == request.auth.token.email ||
        email == callerEmail()
      );
    }

    // ── 1. Users ───────────────────────────────────────────────────
    match /users/{userId} {
      allow read: if isAuth();
      allow create: if isAuth() && request.auth.uid == userId;
      allow update: if isAuth() && (request.auth.uid == userId || isEmployer());
      allow delete: if isEmployer();
    }

    // ── 2. Employees ───────────────────────────────────────────────
    match /employees/{docId} {
      allow read: if true;
      allow create: if isEmployer() || (isAuth() && request.resource.data.email == callerEmail());
      allow update: if isEmployer() || isOwner(resource.data.email);
      allow delete: if isEmployer();
    }

    // ── 3. Leaves ──────────────────────────────────────────────────
    match /leaves/{leaveId} {
      allow read: if isAuth() && (isEmployer() || isOwner(resource.data.empEmail));
      allow create: if isAuth();
      allow update, delete: if isEmployer();
    }

    // ── 4. Payroll ─────────────────────────────────────────────────
    match /payroll/{payId} {
      allow read: if isAuth() && (isEmployer() || isOwner(resource.data.empEmail));
      allow write:  if isEmployer();
    }

    // ── 5. Documents ───────────────────────────────────────────────
    match /documents/{docId} {
      allow read: if isAuth() && (isEmployer() || isOwner(resource.data.empEmail));
      allow create: if isAuth() && (isEmployer() || isOwner(request.resource.data.empEmail));
      allow update: if isAuth() && (isEmployer() || isOwner(resource.data.empEmail));
      allow delete: if isEmployer();
    }

    // ── 6. Attendance ──────────────────────────────────────────────
    match /attendance/{attId} {
      allow read: if isAuth();
      allow create: if isAuth() && isOwner(request.resource.data.empEmail);
      allow update, delete: if isEmployer();
    }

    // ── 7. Tasks ───────────────────────────────────────────────────
    match /tasks/{taskId} {
      allow read: if isAuth();
      allow create, update: if isAuth();
      allow delete: if isEmployer();
    }

    // ── 8. Profile / System ────────────────────────────────────────
    match /profile_updates/{id}   { allow read, write: if isAuth(); }
    match /payslip_requests/{id}  { allow read, write: if isAuth(); }
    match /notifications/{id}     { allow read, write: if isAuth(); }
    match /chat_messages/{id}     { allow read, write: if isAuth(); }
    match /chat_reads/{id}        { allow read, write: if isAuth(); }
    match /leave_balances/{id}    { allow read, write: if isAuth(); }
    match /doc_templates/{id}     { allow read, write: if isAuth(); }
    match /hierarchy/{id}         { allow read, write: if isAuth(); }
    match /announcements/{id}     { allow read: if isAuth(); allow write: if isEmployer(); }

    // ── 9. Employer-Only ───────────────────────────────────────────
    match /companies/{id}         { allow read: if true; allow write: if isEmployer(); }
    match /companySettings/{id}   { allow read: if isAuth(); allow write: if isEmployer(); }
    match /teams/{id}             { allow read: if isAuth(); allow write: if isEmployer(); }
    match /jobs/{id}              { allow read: if isAuth(); allow write: if isEmployer(); }
  }
}
